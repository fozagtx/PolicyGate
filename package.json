{
  "name": "anna-app-policygate",
  "private": true,
  "version": "1.0.0",
  "description": "PolicyGate Anna App bundle for policy-backed case approvals.",
  "scripts": {
    "dev": "anna-app dev",
    "validate": "anna-app validate --strict",
    "test": "vitest run"
  },
  "devDependencies": {
    "@anna-ai/cli": "^0.1.22",
    "vitest": "^2.1.0"
  }
}
